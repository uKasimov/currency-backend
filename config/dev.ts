const config = {
  server: {
    host: "http://localhost",
    port: 8080,
  },
  auth: {
    jwtSecret: "psstitssecret",
  },
  meta: {
    title: "Currency Backend started",
    version: "0.0.1",
    description: "",
  },
};

export default config;
